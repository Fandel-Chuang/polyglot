// polyglot语义错误测试
>> "iostream"

main() {
    // 重复变量声明
    ? x: int = 10
    ? x: float = 3.14  // 错误：重复声明

    // 未声明变量使用
    ? y = unknown_var + 5  // 错误：unknown_var未声明

    // 类型不匹配
    ? name: string = 123  // 错误：类型不匹配

    <- 0
}